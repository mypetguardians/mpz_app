# 2026-04-24 ~ 04-25 작업 이력

## 이벤트(보호센터 홍보 프로젝트) 페이지 구현

### FE
- `/event/centers` 이벤트 페이지 전체 구현 (Figma 기획 기반)
- 인트로 시네마틱 애니메이션 (localStorage, 다음날 09시 리셋)
- 이미지 2장 세로 구성 (새끼강아지 + 10년 후) + dim 처리 (다크브라운)
- 스크롤 연동 배경 페이드 (비율 기반, 갈색 계열)
- 센터 리스트 DB 연동 (`center_type=private` 민간센터 필터)
- 신청 폼: 주소API(다음), 동물수 셀렉트박스, 전체 필수값, 연락처 자동 하이픈
- 해요체 통일, 섹션 제목 18px
- CustomAlert 공통 컴포넌트 (center/bottom, Portal 기반) — alert() 교체
- TopBar/Toast를 Container 밖으로 이동 (transform fixed 포지셔닝 이슈 해결)
- CenterCardSkeleton 실제 카드 구조와 통일
- 홈 배너: 내부 링크 시 router.push 히스토리 이동으로 변경
- 배경 오버레이 max-w-[420px] 모바일 영역 제한

### BE
- `/v1/event/apply` 이벤트 센터 신청 API (네이버 SMTP 이메일 전송)
- 이메일 API 보안: rate limit (IP당 10분 3회), 헤더 인젝션 방지, 에러 로깅
- 이메일 제목에 DEV/PROD 환경 구분 라벨
- `center_type` 필드 추가 (private/public) + 마이그레이션
- `public_reg_no` null → private, 있으면 → public 데이터 마이그레이션
- `center_type` API 필터 추가 (`centers/api/center_api.py`)
- `is_public` 필터 파라미터 추가 (`centers/api.py`)
- settings.py EMAIL SMTP 설정

### Figma MCP
- figma-dev MCP 서버 정상 연동 확인
- 이벤트 페이지 디자인 분석 + 이미지 다운로드 (event-screenshot.png, event-dog-grown.png)

### 인프라
- 로컬 Python 3.12 venv 세팅 (brew install + venv 생성)
- dev 서버 backend/.env에 EMAIL 환경변수 추가
- dev 서버 backend/.env.dev에도 EMAIL 추가 (배포 시 덮어쓰기 대비)

### 기타
- setting-name 페이지 오타(ㅍ) 제거
- 디자인 팔레트 페이지 (/dev/palette, prod 접근 차단)
- PR #11, #12 생성 (mypetguardians/mpz_app)
