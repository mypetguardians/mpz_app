# 2026-04-24 ~ 04-25 작업 이력

## 이벤트(보호센터 홍보 프로젝트) 페이지 구현

### FE
- `/event/centers` 이벤트 페이지 전체 구현 (Figma 기획 기반)
- 인트로 시네마틱 애니메이션 (localStorage, 다음날 09시 리셋)
- 이미지 2장 세로 구성 (새끼강아지 + 10년 후) + dim 처리 (다크브라운)
- 스크롤 연동 배경 페이드 (비율 기반, 갈색 계열)
- 센터 리스트 DB 연동 (`center_type=private` 민간센터 필터)
- 신청 폼: 주소API(다음), 동물수 셀렉트박스, 전체 필수값, 연락처 자동 하이픈
- 해요체 통일, 섹션 제목 18px
- CustomAlert 공통 컴포넌트 (center/bottom, Portal 기반) — alert() 교체
- TopBar/Toast를 Container 밖으로 이동 (transform fixed 포지셔닝 이슈 해결)
- CenterCardSkeleton 실제 카드 구조와 통일
- 홈 배너: 내부 링크 시 router.push 히스토리 이동으로 변경
- 배경 오버레이 max-w-[420px] 모바일 영역 제한

### BE
- `/v1/event/apply` 이벤트 센터 신청 API (네이버 SMTP 이메일 전송)
- 이메일 API 보안: rate limit (IP당 10분 3회), 헤더 인젝션 방지, 에러 로깅
- 이메일 제목에 DEV/PROD 환경 구분 라벨
- `center_type` 필드 추가 (private/public) + 마이그레이션
- `public_reg_no` null → private, 있으면 → public 데이터 마이그레이션
- `center_type` API 필터 추가 (`centers/api/center_api.py`)
- `is_public` 필터 파라미터 추가 (`centers/api.py`)
- settings.py EMAIL SMTP 설정

### Figma MCP
- figma-dev MCP 서버 정상 연동 확인
- 이벤트 페이지 디자인 분석 + 이미지 다운로드 (event-screenshot.png, event-dog-grown.png)

### 인프라
- 로컬 Python 3.12 venv 세팅 (brew install + venv 생성)
- dev 서버 backend/.env에 EMAIL 환경변수 추가
- dev 서버 backend/.env.dev에도 EMAIL 추가 (배포 시 덮어쓰기 대비)

### 기타
- setting-name 페이지 오타(ㅍ) 제거
- 디자인 팔레트 페이지 (/dev/palette, prod 접근 차단)
- PR #11, #12 생성 (mypetguardians/mpz_app)
- 입양 문의: 구독 센터도 임시로 전화 연결로 변경 (앱 내 입양 신청 절차 비활성)

## QA 수정

### 동물 등록/수정 페이지
- 사진 5개 초과 선택 시 토스트 알림 추가
- 견종 선택 아이콘 겹침 수정 (SearchInput → input + CaretDown)
- 성격/특이사항 포커싱 시 테두리 유지 (focus:border-brand)
- 체중 소수점 입력 허용 (0.3 등 중간 상태)
- 나이 → 태어난 날 date picker + 자동 개월 수 계산 (BE 호환 유지)

### 입양 관련
- 입양신청 상세주소 필수값 처리 + 에러 토스트
- 입양 신청 상세 안내 워딩 수정 (해요체)
- 프로필 이름 수정 불가 처리 (readOnly + disabled)

## 디자인 팔레트 통일 (동물 상세 페이지)
- AnimalBasicInfo: 미정의 색상 → 팔레트 색상, flex gap → space-x, mx → px
- WaitingStatus: 하드코딩 hex → orange-100, gap → space-x
- AnimalDetails: text-gray-700 → text-dg, mx → px
- RelatedAnimals: flex gap → margin, mx → px

## GPS 역지오코딩 개선
- Google react-geocode → BigDataCloud 무료 API 전환 (키 불필요)
- 행정구역 경계 기반 정확한 시/도 판별
- findNearestRegion 순수 최단거리 기준으로 변경
- "내 주변" 클릭 → 해당 지역 태그 활성화 (토글 아님)
- 지역 태그 재클릭 해제 비활성화

## SEO 기초 구축
- robots.txt 생성 (관리자/인증 페이지 크롤링 차단)
- 동적 sitemap.ts (정적 페이지 + 동물/센터 상세 전체, 24h 캐시)
- JSON-LD Organization 스키마 추가
- 동물 상세 페이지: Server Component 분리 + generateMetadata (동적 OG 이미지/제목)
- dev 환경 SEO 차단 (robots.ts Disallow: /, sitemap 빈 배열)
- 정적 robots.txt → 동적 robots.ts 전환
- PR #13, #14 생성

## deploy.yml GHCR 고도화
- GitHub Actions에서 Docker 이미지 빌드 → GHCR push → EC2에서 pull만 (EC2 빌드 제거)
- EC2 메모리(4GB) 부족으로 docker build 시 OOM → 서비스 다운 문제 해결
- docker/build-push-action + Buildx GHA 캐시 적용
- GHCR 이미지 태그 소문자 변환 이슈 수정
- EC2 GHCR 로그인 토큰 전달 (envs로 SSH에 GITHUB_TOKEN 전달)
- dev 적용 완료, prod 대기

## UI 수정
- 센터페이지/마이페이지: 프로필 이미지 flex-shrink-0 고정 (닉네임 길어져도 축소 안 됨)
- 센터명 line-clamp-3 (3줄까지 허용, 넘으면 말줄임)
- 마이페이지 닉네임 truncate 처리

## prod 서버 복구
- ESLint 에러 반복 빌드로 EC2 OOM → AWS 콘솔에서 재부팅
- backend Exited 상태 → docker compose start로 복구
- sitemap.xml prod 정상 동작 확인

## deploy.yml 환경변수 수정
- FE 빌드 시 NEXT_PUBLIC_* 환경변수를 .env 파일 대신 브랜치별 직접 분기로 변경
- KAKAO_CLIENT_ID GitHub Secrets 추가
- NEXT_PUBLIC_KAKAO_REDIRECT_URI Dockerfile ARG + deploy.yml build-args 추가
- langchain-openai 버전 수정 (1.0.5 → 1.2.0, 존재하지 않는 버전이었음)
- langchain/langgraph 패키지 버전 고정 (Python 3.12 호환성)

## center_type 자동 세팅
- Center.save()에서 public_reg_no 유무로 center_type 자동 판별
- 공공데이터 센터 → public, 어드민 생성 센터 → private

## 카카오 개발자 콘솔 설정
- 로그인 Redirect URI: localhost:8000 추가
- 불필요 URI 정리: localhost:3000, http://api.mpz.kr 삭제
- 로컬 .env KAKAO_REDIRECT_URI를 localhost:8000으로 변경
- JS SDK 도메인: dev.mpz.kr, localhost:3001 추가 / 127.0.0.1:3000, vercel, api.mpz.kr 삭제
- JS 키 Redirect URI: localhost:8000 추가 / localhost:3000, http://api.mpz.kr 삭제

## 디자인 일관성 수정
- flex gap → space-x/space-y/margin 변경 (7개 파일, CLAUDE.md 규칙 준수)
- Tailwind 기본 색상 → 팔레트 토큰 (blue-500→brand, red-500→error, gray→lg/gr 등)
- H3 font-weight 팔레트 규격 맞춤 (semibold→bold)
- text-lg 예약어 충돌 수정 (text-sm text-lg → text-sm text-gr)
- 닉네임 입력 maxLength=100 + slice 이중 처리 (프로필/센터관리자)

## 에러 페이지 디자인
- not-found.tsx: 길 잃은 강아지 SVG + "앗, 여기는 아무도 없어요"
- error.tsx (신규): 놀란 고양이 SVG + "앗, 문제가 생겼어요" + 다시 시도
- global-error.tsx (신규): 강아지+고양이 SVG + "서비스에 문제가 생겼어요"

## 에러코드 노출 제거 (24개 파일)
- error.message 직접 렌더링 → 유저 친화 한국어 문구로 교체
- alert() → toast/CustomAlert 전환
- 에러 코드(status) UI 노출 제거

## 라이팅 가이드
- frontend/WRITING_GUIDE.md 생성 (브랜드 보이스, 톤앤매너, 레벨링 1~5)
- CLAUDE.md 규칙 19번 추가 (라이팅 가이드 참조)

## Footer 정리
- 불필요 영역 제거 (로고/슬로건/빠른링크/디바이더)
- 사업자 정보만 깔끔하게 정돈
- 시맨틱 footer 태그 적용

## Pretendard 폰트 적용
- CDN dynamic-subset (실사용 글리프만 로드, 번들 최소화)
- tailwind fontFamily.sans + globals.css font-family 설정

## GNB 스타일 개선
- 비활성 아이콘/텍스트 색상: #e3e3e3 → #cccccc (가시성 개선)
- 활성 텍스트: font-medium → font-bold

## deploy.yml prod 적용
- dev → main 머지, prod GHCR 배포 성공
- PR #15 생성 (mypetguardians/mpz_app)
