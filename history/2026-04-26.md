# 2026-04-26 작업 이력

## 불필요 코드/리소스 정리
- 빈 Django views.py 14개 제거 (Ninja API 사용, views 미사용 확인)
- 레거시 centers/api.py 제거 (centers/api/ 디렉토리 패키지가 Python import 우선)
- 미사용 이미지 6개 제거 (Badge SVG 5 + event-description PNG)
- 미사용 FE 패키지 6개 제거 (drizzle-orm/kit, better-sqlite3, react-geocode/geolocated, tw-animate-css)
- drizzle.config.ts 제거, tw-animate-css 주석 제거
- 개발 스크립트 8개 + hello_world.py 제거
- 빈 local-cdn/ 디렉토리 제거
- 총 1,522줄 삭제

## Firebase 새 프로젝트 생성 + 환경변수 세팅
- 이전 개발사 Firebase 프로젝트(mpz-app-b2e01) → 새 프로젝트(mypetguardians-a8ad3) 생성
- mypetguardians@gmail.com 계정으로 Firebase 콘솔에서 직접 생성
- FCM V1 API 활성화, 서비스 계정 키 JSON 다운로드, VAPID 키 생성
- dev 서버 .env + .env.dev에 FIREBASE_ADMIN_CREDENTIALS_JSON, FCM_PROJECT_ID 세팅
- Docker env_file은 컨테이너 생성 시에만 읽힘 → restart가 아닌 down+up 필요 (재발견)
- FCM dry-run + 실제 발송 테스트 성공 (200 OK)

## Firebase 웹 푸시 알림 구현
- Firebase JS SDK 설치 (pnpm add firebase, v12.12.1)
- Firebase 초기화 모듈 (src/lib/firebase.ts) — 싱글톤, SSR 안전, isSupported() 체크
- Service Worker (public/firebase-messaging-sw.js) — 백그라운드 알림 수신 + 클릭 핸들러
- usePushToken.tsx 웹 플랫폼 분기 추가 (getWebFCMToken + Notification.requestPermission + SW 등록)
- SocketProvider 포그라운드 메시지 수신 (onMessage → toast + CustomEvent 발행)
- Dockerfile + deploy.yml Firebase 환경변수 7개 추가 (ARG/ENV + build-args)
- BE web 플랫폼 FCM 페이로드 (아이콘, 클릭 링크)
- Capacitor 네이티브 vs 웹 브라우저 완전 분기 (detectPlatform 기반)

## FCM TTL + collapse_key 설정
- 미전달 메시지 1시간 후 폐기 (TTL 3600초) — 브라우저 닫았다 열 때 밀린 알림 한꺼번에 오는 문제 방지
- notification_type별 collapse_key — 같은 타입 알림은 최신 1개만 전달
- iOS: apns-expiration + apns-collapse-id
- Android: ttl + collapse_key
- Web: TTL + Topic 헤더

## 알림 UI 개선
- NotificationToast: bottom → top 위치 변경, 슬라이드 다운 입장/퇴장 애니메이션 (300ms)
- push 타입 추가 (Bell 아이콘, brand 색상, FCM 포그라운드 전용)
- NotificationCard: 타입별 아이콘 (입양=PawPrint, 커뮤니티=ChatCircle, 좋아요=HeartStraight, 모니터링=Eye)
- 읽지 않은 알림 dot 표시 (brand 색상 원)
- 폰트 규격 정리 (타입 11px, 메시지 14px, 시간 11px)
- useGetNotifications: refetchOnWindowFocus 활성화 (백그라운드 푸시 후 탭 복귀 시 뱃지 갱신)

## HomeHeader + NotificationBadge 뱃지 실시간 업데이트
- FCM 포그라운드 메시지 수신 시 CustomEvent("fcm-notification-received") 발행
- HomeHeader에서 이벤트 수신 → fcmBump 카운트 증가 + refetch
- NotificationBadge에서도 이벤트 수신 → fcmBump 카운트 증가
- QueryClientProvider 순서 이슈 해결 (SocketProvider에서 useQueryClient 제거 → 이벤트 기반)

## GPS 위치 확인 대기시간 단축
- accuracyThreshold: 50m → 200m (Wi-Fi 기반 브라우저 대응)
- watchTimeout: 12초 → 5초
- 데스크톱 브라우저에서 "위치 확인 중..." 상태가 12초→5초로 단축

## 발견된 이슈 (다음 작업)
- Auth 상태 깜빡임(flash): 새로고침 시 isLoading=true 동안 로그아웃 UI 표시 후 전환
  - HomeHeader, NavBar, MyPage, CenterPage, FavoriteLayout, ListLayout 등 미처리
  - PetSection, Banner, AnimalTab, Notifications는 이미 스켈레톤 적용됨
- env/시크릿 관리 개선 필요 (GitHub Secrets 통합 방향 결정됨)
- Chrome GCM 연결이 CONNECTING 상태로 고착되는 현상 (Chrome 재시작으로 해결)

## Auth 상태 깜빡임 해결
- HomeHeader: isAuthLoading 중 빈 공간 표시 (로그인 버튼 깜빡임 방지)
- FavoriteLayout/ListLayout: authLoading 중 TopBar right 빈 공간
- MyPage: 프로필 닉네임 스켈레톤 + 수정 버튼 숨김
- CenterPage: 센터명 스켈레톤
- NavBar: authLoading 중 찜/마이 탭 클릭 무시 (로그인 모달 오발동 방지)

## 지역 태그 스크롤 포커싱
- "내 주변" 클릭 → GPS 지역 감지 → 해당 지역 태그로 가로 스크롤 smooth 애니메이션

## SEO 기초 인프라
- Google Search Console 인증 (HTML 메타태그) + sitemap 제출 (8,112 페이지 발견)
- 네이버 서치어드바이저 인증 + sitemap 제출
- layout.tsx verification 메타태그 삽입 (Google + Naver)
- prod 배포 완료 (main push + PR #16)

## 센터 상세 동적 OG 메타데이터
- page.tsx를 Server Component로 전환 + generateMetadata 추가
- 센터명, 지역, 설명, 이미지를 동적 OG/Twitter 메타데이터로 생성
- 클라이언트 로직을 CenterDetailClient.tsx로 분리

## JSON-LD 구조화 데이터
- 동물 상세: Thing 스키마 (이름, 품종, 나이, 성별, 이미지, URL)
- 센터 상세: AnimalShelter 스키마 (이름, 설명, 주소, 연락처, 이미지)

## 시맨틱 HTML + 접근성 개선
- Container: div → main 시맨틱 태그
- NavbarBtn: div → button + aria-label + aria-current
- CenterCard: role="link" + tabIndex + aria-label + Enter 키 지원
- AnimalTab/AnimalSearchSection: 카드 래퍼 role="link" + tabIndex + Enter 키
- notifications: h6 onClick → button (시맨틱 오류 수정)

## env/시크릿 관리 연구
- 현재 .env + .env.dev 이중 파일 구조 문제점 분석
- 방법 비교: 현재 유지, GitHub Secrets, AWS SSM, Secrets Manager, Docker Secrets, SaaS
- 결정: GitHub Secrets → deploy.yml에서 .env 생성 방식 (2단계, 비용 0)
- Firebase JSON은 별도 파일 마운트 방식 권장
