# 2026-04-27~28 작업 내역

## 홈페이지 SEO h1 태그 추가
- `page.tsx`에 `<h1 className="sr-only">마펫쯔 - 유기동물 입양 플랫폼</h1>` 추가
- 다른 페이지는 TopBar 컴포넌트가 이미 h1 태그 렌더링 → 홈만 누락이었음
- PR #25 포함

## 스크롤바 콘텐츠 영역 제한
- SafeAreaLayout: `min-h-screen` → `h-screen overflow-hidden flex-col`
- Container: `min-h-full` → `flex-1 overflow-y-auto`
- 스크롤바가 헤더/NavBar 영역에 걸치지 않도록 수정

## 입양 목록 UX 전면 개선
- **검색 영역 스크롤 hide/show**: 스크롤 내리면 검색+필터 영역 접힘, 올리면 펼침 (maxHeight 애니메이션)
- **실시간 검색**: 입력할 때마다 즉시 검색 결과 표시 (엔터 불필요)
- **통합 검색**: BE에 `search` 파라미터 추가 (품종+이름+발견장소 OR 검색)
- **필터 UI**: 필터 초기화 버튼, 기본값(입양가능)은 활성 필터로 미표시
- **품종 필터**: 리스트에 없는 품종명 선택 차단, 돋보기→X버튼+쉐브론 나란히 배치
- **SearchInput**: X 버튼 초기화 기능 추가 (onClear prop)
- **searchValue persist 제거**: 새로고침 시 검색 초기화
- PR #25~#29 포함

## 보호센터 탭 무한스크롤 수정
- CenterTab: `useWindowVirtualizer` → `useVirtualizer` (scroll container 기반)
- CenterSearchSection: `window` scroll → `scrollContainerRef` 기반 무한스크롤
- CenterCard: 긴 이름 겹침 수정, 인증마크 크기 고정 (`flex-shrink-0`), 간격 조정

## 찜 상태 Batch API 추가
- BE: `POST /v1/favorites/animals/batch-status` 엔드포인트 추가
- FE: `useBatchAnimalFavorites` 훅 추가
- 동물 목록에서 개별 찜 조회(N콜) → 일괄 조회(1콜)로 변경
- **UUID 타입 불일치 버그 수정**: `values_list`가 UUID 객체 반환 → `str()` 변환 추가
- 찜 토글 초기 상태를 batchFavorites에서 참조하도록 수정
- 검색 결과에도 batch-status 적용

## DB 연결 풀 안정성 (CONN_MAX_AGE)
- **원인**: `CONN_MAX_AGE=300` + UvicornWorker(async) → 연결 5분간 미반환 → 200개 한계 도달
- **트리거**: 실시간 검색(입력마다 API 호출)으로 동시 연결 폭증
- **수정**: dev+prod `CONN_MAX_AGE=0` (pgbouncer 6543 경유이므로 Django가 연결 붙잡을 필요 없음)

## axios 요청/응답 로그
- axios 인터셉터에 공통 요청/응답 콘솔 로그 추가
- `[요청] POST https://...` / `[응답] POST ... 200` 형식

## 유틸 함수
- `numberWithComma`: 숫자에 천 단위 콤마 추가 (format-utils.ts)

## 이미지 경량화 + 마이그레이션 (구현 완료, 배포 전)
- 동기화 시 이미지 자동 경량화: `_optimize_image()` (1080px, JPEG 85%)
- `migrate_images` 커맨드: 기존 공공API URL 이미지 788개 일괄 마이그레이션
- 예상 용량: ~80MB 추가 (Pro 100GB 한도 대비 여유)

## 검색 영역 hide/show 분리
- 검색+필터 영역을 스크롤 컨테이너 밖으로 분리
- AnimalSearchSection → useAnimalSearch 훅 전환 (controlArea/resultsArea 분리 반환)
- 스크롤 내리면 접힘, 올리면 펼침 (maxHeight + opacity transition)
- search/breed 파라미터 분리 (통합검색과 필터 분리)

## 검색 결과 버추얼 스크롤
- 검색 결과에도 useVirtualizer 적용 (2열 row 단위, AnimalTab과 동일 패턴)
- window scroll listener 제거 → virtualizer 기반 무한스크롤

## 센터 Batch Favorites API
- BE: `POST /v1/favorites/centers/batch-status` 추가
- FE: `useBatchCenterFavorites` 훅 + CenterTab 적용
- CenterCardWithFavorite에서 개별 useCheckCenterFavorite 제거

## SearchInput X버튼 + 품종 필터 개선
- SearchInput: onClear prop 추가, 입력값 있으면 X버튼 / 없으면 돋보기
- BreedFilter: 리스트에 없는 품종 선택 차단, 돋보기→X+쉐브론 나란히 배치

## 코드 자체 검토 (FE/BE/인프라)
- 프론트엔드 6.6/10, 백엔드 7/10, 인프라 7.5/10
- 주요 이슈: 거대 컴포넌트, React.memo 미사용, Nginx SSL/gzip, JWT OneToOneField
- 상세 리포트는 대화 내역 참고

## 배포
- PR #25~#31 dev 배포
- dev 빌드 실패 2회 (package-lock 미동기화, ESLint 미사용 변수) → 수정 후 성공
- CONN_MAX_AGE=0 dev+prod EC2 직접 반영
