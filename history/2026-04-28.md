# 2026-04-28 작업 내역 (코드 품질 개선)

## P0 완료

### PetCard Compound Components 분해
- PetCard.tsx 540줄 → 67줄 (라우터만)
- pet-card/usePetCardData.ts: 공통 로직 훅
- pet-card/StatusChip, GenderBadge: 공통 서브 컴포넌트
- PetCardPrimary~Edit: variant별 개별 컴포넌트
- Props 인터페이스 변경 없음, 사용처 수정 0건

### 카카오 프로필 이미지 async 전환
- parse_kakao_profile → async def
- httpx.Client(동기) → httpx.AsyncClient
- 호출부 2곳 await 추가

### 동기화 센터 N+1 캐싱
- process_abandoned_animals에서 _center_cache dict 도입
- 같은 센터 반복 조회 방지 (1000콜→수십콜)

### Docker 로그 드라이버
- 5개 서비스에 json-file 드라이버 + max-size 10m + max-file 3

## P1 완료

### 이미지 로딩 9배 개선
- next.config.ts: unoptimized: true
- Next.js 서버 변환(0.63초) 스킵 → Supabase 직접 서빙(0.07초)

### Nginx 강화
- HTTP/2 활성화 (prod/dev 4곳)
- 보안 헤더 5종 (HSTS, nosniff, SAMEORIGIN, XSS, Referrer)
- gzip 압축 (JSON, JS, CSS, SVG)

### Docker 강화
- 헬스체크 (backend curl, redis ping)
- 메모리 제한 (backend 1536M, frontend 512M, worker 512M, redis 256M, nginx 256M)
- healthcheck timeout 15s, start_period 60s

### React.memo
- AnimalCardWithFavorite: animal.id, localFavorite, batchFavorite 커스텀 비교
- CenterCardWithFavorite: 동일 패턴

### useScrollVisibility Hook 분리
- ListLayout에서 스크롤 로직 추출 → 독립 훅

### TanStack Query 키 정규화
- JSON.stringify 제거 → normalizeAnimalParams 함수
- 필드 순서 고정으로 캐시 히트율 개선

### BE 인증 DI 패턴
- get_authenticated_user() 헬퍼 함수
- favorites/api.py 9개 엔드포인트 적용

### BE 에러 응답 표준화
- str(e) 노출 제거 → logger.exception + 제너릭 메시지

### JWT 만료 단축 + 자동 갱신
- ACCESS 60년→2시간, REFRESH 120년→30일
- axios interceptor: 401 → refresh cookie 자동 갱신 → 재요청
- 동시 401 refresh 중복 방지 (refreshPromise 싱글턴)
- auth:expired 이벤트 → AuthProvider 자동 로그아웃
- BE refresh-token: 쿠키 읽기 지원

### Rolling 배포
- Blue-Green 시도 → Docker Compose alias 충돌로 502 → 즉시 복구
- Rolling 방식으로 변경 (backend→frontend→nginx 순차)

## P2 완료

### FCM 리스너 분리
- page.tsx 모듈 최상단 코드 → FCMTokenListener.tsx 컴포넌트

### Pydantic v2 model_config
- PersonalityTestIn: Config → ConfigDict(json_schema_extra=...)

### 느린 쿼리 로깅
- django.db.backends 로거 추가 (WARNING, DEBUG로 변경 시 쿼리 출력)

### Frontend Dockerfile non-root
- nextjs user 생성 + USER 지정 + chown

### 배포 후 smoke test
- FE/BE 200 확인 (10회×3초)

## 기타

### prod 콘솔 전체 억제
- 환영 메시지(강아지 ASCII + 랜덤 문구) 출력 후 console 전부 noop

### 보호센터 상세 보호동물 무한스크롤
- page: 1 고정 제거 → IntersectionObserver 무한스크롤

### event/centers TopBar 가림 수정
- Container에 pt-[54px] 추가

### 코드 최종 검토
- FE 14파일 + BE/인프라 12파일 전수 검토
- 메모리 상향, healthcheck 강화, smoke test 개선

## 배포
- PR #42~#60 dev+prod
- Blue-Green 502 장애 발생 → 5분 내 복구
- ESLint 빌드 실패 1회 (미사용 import) → 수정
