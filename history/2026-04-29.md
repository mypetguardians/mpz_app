# 2026-04-29 작업 내역

## SMS 인증 (알리고 전환)

### BE
- `sms/client.py` — 알리고 API 클라이언트 (async httpx, testmode 환경변수 분리)
- `phone_verification.py` — Railway blink 제거 → 알리고 직접 호출
- `settings.py` — ALIGO_API_KEY, ALIGO_USER_ID, ALIGO_SENDER, ALIGO_TESTMODE 환경변수
- `result_code` 문자열/정수 비교 버그 수정 (`== 1` → `int() > 0`)
- dev/prod EC2 + GitHub Secrets 환경변수 반영

### FE
- Step1 (입양신청): FE 인증번호 생성(보안 취약) → BE API 기반으로 전면 교체
- 프로필 수정: 단순 입력 → SMS 인증 절차 (발송→OTP→확인, 3단계 stage)
- 입양 상세 내정보: "전화번호 정보 없음" 하드코딩 → `user.phoneNumber` 표시 (5곳)
- `usePhoneVerificationFlow` 훅: 발송/재전송/검증/타이머/에러 공통 로직 추출
- 인증번호 만료 UX: 입력 비활성화 + "만료됨" + 재전송 강조
- 버튼 입력 필드 아래 오른쪽 정렬
- 빈 번호 시 토스트, 토스트 3초 자동 dismiss

### 교훈
- **보안**: FE에서 인증번호 생성 + sessionStorage 비교는 심각한 보안 취약점. 반드시 BE에서 생성/검증
- **알리고 발송 IP**: EC2 IP만 등록돼서 로컬에서는 실제 발송 안 됨

### .env 파일 사고
- **원인**: SSH로 `.env.dev`에 알리고 환경변수 추가 시 기존 내용 덮어쓰기
- **영향**: dev+prod .env 날아감 → BE 크래시 → GitHub Secrets도 오염
- **복구**: `backend/.env` 원본에서 복원, Secrets 재설정
- **재발 방지**: CLAUDE.md 규칙 #12 추가 (EC2 파일 수정 시 절대 덮어쓰기 금지)

---

## 모바일 뷰포트 잘림 수정
- SafeAreaLayout `h-screen`(100vh) → `height: 100svh`
- 모바일 주소창 show/hide 시 레이아웃 출렁임 해결

## 미사용 패키지 제거
- @cloudflare/workers-types, @hono/zod-openapi, better-auth, lucide-react 제거
- auth-client.ts, auth-utils.ts dead 파일 삭제
- dialog.tsx: lucide XIcon → phosphor X 교체
- drizzle-kit scripts 제거

## Lottie 로딩 애니메이션
- lottie-react 패키지 추가
- Loading 컴포넌트: fullScreen(`absolute inset-0` 가운데), sm/md/lg 사이즈
- 브랜드 블루(#3e93fa) 발자국 애니메이션
- Suspense fallback 13곳 + 주요 페이지 로딩 8곳 적용
- **빌드 실패 교훈**: src/assets/ JSON 파일 git add 누락 → Docker 빌드 실패

## EmptyState 컴포넌트
- `absolute inset-0` 가운데 정렬 + 아이콘 + 제목 + 설명
- 동물/센터 목록, 찜 동물/센터, 입양 신청 5곳 적용

## Toast 위치 통일
- Toast 컴포넌트에 `fixed bottom-[88px]` 기본 내장
- 사용처에서 별도 위치 className 불필요

## 찜 탭 개수 표시
- FavoriteLayout 탭 라벨에 numberWithComma 개수 표시
- 본문 "총 N개" 텍스트 제거

## 로컬 개발 환경 정비
- Makefile: `.env` 복사 후 카카오 redirect_uri를 localhost로 자동 교체
- `make setup` 추가 (최초 세팅 자동화)
- macOS/Linux 크로스 플랫폼 sed 지원
- `.env.dev`/venv 없을 때 에러 안내
- **로컬 카카오 로그인 실패 원인**: redirect_uri 불일치 (dev-api vs localhost), cross-domain 쿠키 제한
- **해결**: `make dev`에서 자동 교체 + FE/BE 동시 localhost 실행

## 강아지학교 분석
- 코드베이스 분석 완료 (스택, DB 스키마 23개 테이블, 기능)
- 통합 방향 확정: 유저 마스터=마펫쯔, Supabase 공유(ds_ prefix), 실시간 동기화
- 도메인: school.mpz.kr
- 약관/탈퇴 프로세스 설계 (현행법 기준)

## 배포
- PR #73~#82 dev 배포
- PR #78, #80 prod 배포
- 빌드 실패 2회: package-lock.json 누락 + Lottie JSON git 미추적
